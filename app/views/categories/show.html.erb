<p style="color: green"><%= notice %></p>

<%= render @category %>

<div>
  <%= link_to "Edit this category", edit_category_path(@category) %> <br>
  <%= button_to "Delete", @category, method: :delete %>
  <%= link_to "Back to categories", categories_path %>

</div>
