<% content_for :title, "Categories" %>

<h1>Categories:<h1>

<div>
  <% @categories.each do |category| %>
    <ul>
      <li style="font-size: 25px;"><%= category.task %></li>
      <span style="font-size: 18px;"><%= link_to "Show this task", category %></span>
    </ul

  <% end %>

</div>
<br>

<%= link_to "New category", new_category_path %>