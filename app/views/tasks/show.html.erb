<div>
  <p>
    Task:
    <%= @task.task%>
  </p>

  <p>
    Description:
    <%= @task.description%>
  </p>
</div>

<%= link_to "Edit", edit_category_task_path(@category, @task)%>
<%= button_to "Delete", [@category, @task], method: :delete%>
<%= link_to "Back to tasks", category_tasks_path(@category)%>